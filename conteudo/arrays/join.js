const grupo1 = [
    'join', 'reverse', 'sort', 'push', 'pop', 'shift', 'unshift',
    'splice', 'concat', 'slice', 'indexOf', 'lastIndexOf',
    'forEach', 
]

const grupo2 = ['map', 'reduce', 'reduceRight', 'filter',
'some', 'every', 'isArray']

const grupoUnif = grupo1.concat(grupo2);

console.log(grupoUnif);